<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
    xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc    http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans  http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- @Controller 활성화 -->
    <mvc:annotation-driven />

    <!-- 정적 리소스 -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    <mvc:resources mapping="/uploads/**"  location="/uploads/" />

    <!-- 기본 서블릿 위임 -->
    <mvc:default-servlet-handler />

    <!-- Tiles 설정 -->
    <beans:bean id="tilesConfigurer" class="org.springframework.web.servlet.view.tiles2.TilesConfigurer">
        <beans:property name="definitions">
            <beans:list>
                <beans:value>classpath:tiles/*.xml</beans:value>
            </beans:list>
        </beans:property>
        <beans:property name="preparerFactoryClass"
                        value="org.springframework.web.servlet.view.tiles2.SpringBeanPreparerFactory" />
    </beans:bean>

    <!-- Tiles ViewResolver -->
    <beans:bean id="viewResolver" class="org.springframework.web.servlet.view.UrlBasedViewResolver">
        <beans:property name="viewClass" value="org.springframework.web.servlet.view.tiles2.TilesView" />
    </beans:bean>

    <!-- 컴포넌트 스캔 (Controller/Service 등) -->
    <context:component-scan base-package="com.myspring.vampir" />

    <!-- 인터셉터 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/*/*.do" />
            <beans:bean class="com.myspring.vampir.common.interceptor.ViewNameInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 파일 업로드 -->
    <beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <beans:property name="defaultEncoding" value="UTF-8" />
        <beans:property name="maxUploadSize" value="52428800" />
        <beans:property name="maxInMemorySize" value="52428800" />
    </beans:bean>

    <!-- === 여기가 핵심: MyBatis 매퍼 스캔 추가 ===
         주의: sqlSessionFactoryBeanName 값은 너의 프로젝트에서 쓰는 팩토리 빈 이름과 같아야 함.
         (보통 'sqlSessionFactory'를 많이 씀. 다르면 그 이름으로 바꿔줘) -->
    <beans:bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!-- 자유게시판 매퍼 패키지 추가 -->
        <beans:property name="basePackage"
                        value="com.myspring.vampir.freeboard.mapper" />
        <beans:property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </beans:bean>

</beans:beans>
